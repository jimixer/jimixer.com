# AWS Profile for jimixer.com project
# Copy this file to .envrc and configure with your AWS credentials

# Clear environment variable credentials (use the SSO profile instead)
unset AWS_ACCESS_KEY_ID
unset AWS_SECRET_ACCESS_KEY

# AWS Profile: IAM Identity Center の権限セット JimixerComGalleryOps。
# 恒久的なアクセスキーは使わない。`aws sso login --sso-session jimixer` でログインする。
#
# ここでは既定の（狭いほうの）プロファイルだけを置く。広い権限が要るコマンドは
# コマンド自身が --profile で宣言する（infrastructure/package.json の deploy）。
#
# `AWS_PROFILE=jimixer-admin direnv exec . ...` のような前置きは効かない。
# direnv exec は .envrc を読む前に direnv の状態を巻き戻すため、前置きした値も
# 一緒に破棄される。呼び出し側の環境に依存させないこと。
export AWS_PROFILE=jimixer-gallery

# AWS Account and Region for CDK
export AWS_ACCOUNT_ID=YOUR_AWS_ACCOUNT_ID
export AWS_REGION=ap-northeast-1

# ACM Certificate ARN (must be in us-east-1 for CloudFront)
export CERTIFICATE_ARN=arn:aws:acm:us-east-1:YOUR_ACCOUNT_ID:certificate/YOUR_CERTIFICATE_ID

# Optional: Domain name (defaults to jimixer.com)
# export DOMAIN_NAME=jimixer.com
